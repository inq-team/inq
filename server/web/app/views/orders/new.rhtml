<h1>New order</h1>

<%= error_messages_for :order %>

<% form_for(:order, :url => orders_path) do |f| %>
  <p>
    <b>Doc no</b><br />
    <%= f.text_field :doc_no %>
  </p>

  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>

<%= link_to 'Back', orders_path %>