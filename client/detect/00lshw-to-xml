#!/bin/sh
# NAME=lshw to xml converter
# DESCRIPTION=Convert lshw output to required XML document.
# DEPENDS=Video,NIC,Fire Wire

lshw -xml |
	sed 's/[[:cntrl:]]//g' |
	xsltproc $SHARE_DIR/lshw.xslt - |
	sed '/^ *$/d' > ~/components.xml
